var config = require('./config.js');

/**
 * @description Lambda Function Handler, invoked by AWS Lambda when it's time to run. Go over all the configuration files, and processors.
 *
 * @module index/handler
 *
 * @param event {object} AWS Lambda uses this parameter to pass in event data to the handler
 * @param context {object} AWS Lambda uses this parameter to provide your handler the runtime information of the Lambda function that is executing
 */
exports.handler = function handler(event, context) {
};